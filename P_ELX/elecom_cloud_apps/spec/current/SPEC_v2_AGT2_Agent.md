---
project: AdminLink
version: "2.0"
date: "2026-03-13"
source: SPEC.xlsx
total_requirements: 315
primary_language: en
secondary_language: ja
product_types: [switch, access_point]
status: review
part: 2
topic: agent
---

<!-- ============================================================
 AI PARSING INSTRUCTIONS
 1. This document is a requirements specification.
    `- [ ]` checkboxes represent ACCEPTANCE CRITERIA, not incomplete tasks.
 2. English text is the AUTHORITATIVE version.
    Japanese text inside <details> blocks is supplementary translation only.
 3. Requirements preceded by "⚠️ DEPRECATED" or wrapped in <del> tags
    have been removed — do NOT implement them.
 4. Cross-references like "(Refer AGT.x.y)" point to sections within this file.
 5. "UI Message sheet", "Log Message sheet", and all "EJxx" document references
    are external documents NOT included in this file.
 6. Terminology: "AdminLink" = cloud management service;
    "Agent" = on-device software component;
    "device" = the managed network device (switch or access point).
 7. Valid device registration states:
    Disabled | Unregistered | Registered | Checking | re-registerable
============================================================ -->

# AdminLink Requirements Specification

## Client Request

**EN:** Client wants to use the AdminLink service.  

---

## Glossary

| Term | Definition |
|------|-----------|
| AdminLink | The cloud-based device management service |
| Agent | The on-device software component that communicates with AdminLink |
| Device ID | UUID v4 generated by the device; used as identifier in all API calls |
| Registration Code | A 9-digit hex code generated for device registration (`register code` / `regist_cd`) |
| dev_id_changed | Flag (0 or 1) in API response; 1 indicates the device has been factory-reset after prior registration |
| agt_daily_sec | Seconds offset from 22:00:00 for daily processing execution time |
| agt_upload_sec | Seconds offset from :00:00 for hourly status upload timing |
| tmp_reg_expiry | Temporary registration expiry datetime (Zero-touch flow only) |
| re-registerable | State where a device was previously registered, factory-reset, and can be re-registered |
| Periodic Notification | `sts_type = 0` — status update sent by scheduled hourly process |
| Event Notification | `sts_type = 1` — status update triggered by an event |
| Optional Notification | `sts_type = 2` — status update triggered manually |

## Device Registration State Machine

| State | Description | Triggered By | Transitions To |
|-------|-------------|-------------|----------------|
| `Disabled` | AdminLink function is turned off | User disables via Web UI | `Unregistered` (when re-enabled) |
| `Unregistered` | Not registered to AdminLink service | API returns 401; factory reset | `Registered`, `Checking` |
| `Registered` | Successfully registered | API returns 201 or 200 (dev_id_changed=0) | `Unregistered`, `Checking`, `re-registerable` |
| `Checking` | Registration status cannot be determined | API returns other error; no response | `Registered`, `Unregistered` (after retry) |
| `re-registerable` | Factory-reset after registration | API returns 200 (dev_id_changed=1) | `Registered` (after re-registration) |

---

> **Cross-file references:**
> - AGT.1 (Web UI) → `SPEC_v2_AGT1_WebUI.md`
> - AGT.2 (Agent) → `SPEC_v2_AGT2_Agent.md`
> - AGT.3 (Remote Control) → `SPEC_v2_AGT3_RemoteControl.md`
> - AGT.4 (Zero-Touch) → `SPEC_v2_AGT4_ZeroTouch.md`

---

# AGT.2 — As long as the AdminLink function is enabled and registered, the agent should continue to function and it must be able to monitor events and perform periodically process.

**Rationale:** 
- Client wants to monitor events.
- Client wants to process periodically.

## AGT.2.1 — Must be able to start the AdminLink agent function

**Rationale:** Client wants to start the AdminLink agent function.

### AGT.2.1.0 — Start the agent function at the device startup

#### `AGT.2.1.1`

The agent function willl start if the AdminLink function is "enabled"at the device startup.
The agent function will not start, if the AdminLink function is "disabled."

#### `AGT.2.1.2`

Log if the agent function starts.

### AGT.2.1.10 — Start the agent function when the AdminLink function is changed from disabled to enabled.

#### `AGT.2.1.11`

The agent function will start if the AdminLink function changes from disabled to enabled by the Web UI's operation.

#### `AGT.2.1.12`

Log if the agent function starts.See the 'Log Message' sheet.

> **⚠️ DEPRECATED** — The requirements below have been removed and must NOT be implemented. Retained for historical reference only.

<del>
#### `AGT.2.1.13`

If the configuration file includes device's MAC address information,
Note: This case, delete the device registration information on AdminLink server.

#### `AGT.2.1.14`

If the configuration file includes device's MAC address information,
When AdminLink agent starts with configuration file, and the MAC address on configuration file is different from one on the device, log the message.
See the 'Log Message' sheet.

</del>

#### `AGT.2.1.15`

To prevent AdminLink function from the malfunction, initialize the variables and work space  that the agent uses,

### AGT.2.1.20 — Check the device registration status when the agent function starts.

#### `AGT.2.1.21`

Check the device registration status by the process of AGT.1.4.

### AGT.2.1.30 — When the registration status of the device is "Checking".

#### `AGT.2.1.31`

If the device registration status is "Checking", up to 6 times, retry checking the device registration status until the status becomes something other than "Checking".

### AGT.2.1.40 — When the device registration status is **"Registered"**.

#### `AGT.2.1.41`

Start periodical process.
Start remote control reception.

### AGT.2.1.50 — When the registration status of the device is "Unregistered".

> **⚠️ DEPRECATED** — The requirements below have been removed and must NOT be implemented. Retained for historical reference only.

<del>
#### `AGT.2.1.51`

If the device is unregisterd to the AdminLink service, stop if periodical process is being performed.
Stops if remote control is being accepted.

#### `AGT.2.1.52`

Log the stop of an agent function. See the 'Log Message' sheet.

</del>

## AGT.2.2 — Must be able to stop the AdminLink agent function

**Rationale:** Client wants to stop the AdminLink agent function.

### AGT.2.2.0 — Stop the agent function when the AdminLink function changes from enabled to disabled.

#### `AGT.2.2.1`

When the AdminLink function changes from enabled to disabled by Web UI operation, the agent function will stop.

#### `AGT.2.2.2`

Log  when the agent function stops.See the 'Log Message' sheet.

## AGT.2.3 — Must be able to carry out daily processing (every 24 hours)

### AGT.2.3.0 — Start daily processing

#### `AGT.2.3.1`

When the AdminLink function is enabled and the device registration status is **"Registered"** or **"Checking"**, the daily processing is executed once every 24 hours.

When the device does not have the value of The **"number of seconds for calculating the agent daily processing execution time"** (the value of key **"agt_daily_sec"** in the response body of device registration Web API), don't execute daily processing

#### `AGT.2.3.2`

Daily processing starts at 22:00:00, and at the time added by the **"number of seconds for calculating the agent daily processing execution time"**. The "number of seconds for calculating the agent daily processing execution time" is the value of the **"agt_daily_sec"** key included in the response body of the device registration Web API (this value is in seconds).

For the datail about **"agt_daily_sec"**, refer to the sheet "2.2.Device registration API" on the document "EJ03.(AdminLink) 01_ WebAPI Specifications".

For the datail about timing to Daily processing, refer to the sheet "AGT.2.3.2" on the document "EJ105_AdminLinkRequestSpecifications_APSW".

#### `AGT.2.3.3`

Log the daily processing start. See the 'Log Message' sheet.

### AGT.2.3.10 — Check the device registration status.

#### `AGT.2.3.11`

Check the device registration status.
But, if any other process to check the device registration status is ongoing, don't execute this process.

By same methond as one of AGT.1.4.

### AGT.2.3.20

#### `AGT.2.3.21`

Log the result of the registration status confirmation by daily processing as **"Checking"**.

### AGT.2.3.30 — When the device registration status is **"Registered"**.

#### `AGT.2.3.31`

Log the result of the registration status confirmation by daily processing as **"registered"**.

### AGT.2.3.40 — When the device registration status is "Unregistered".

#### `AGT.2.3.41`

Stop periodical process if it is running.
Stop remote control reception.

#### `AGT.2.3.42`

Log the fact that the agent function will stop when it is **"unregistered"**, due to the registration status confirmation by daily processing. See the 'Log Message' sheet.

## AGT.2.4 — The device status information must be acquired hourly and sent to the AdminLink server.

**Rationale:** Client wants to get the device status.

### AGT.2.4.0 — Start hourly status check

#### `AGT.2.4.1`

When the AdminLink function is enabled and the device registration status is **"Registered"** or **"Checking"**, this function will be executed periodically every hour.

#### `AGT.2.4.2`

Starting from 00 minutes 00 seconds every hour, it starts from the time when the **"number of seconds for agent information transmission timing calculation"** is added, and is executed every hour thereafter.

The **"number of seconds for calculating the timing for sending agent information"** is the value of the **"agt_upload_sec"** key included in the response body of the device registration Web API (this value is in seconds).

For the datail about **"agt_upload_sec"**, refer to the sheet "2.2.Device registration API" on the document "EJ03.(AdminLink) 01_ WebAPI Specifications".

For the datail about timing to hourly status check, refer to the sheet "AGT.2.4.2" on the document "EJ105_AdminLinkRequestSpecifications_APSW".

#### `AGT.2.4.3`

Generate JSON data at scheduled time, though the **"Send device information"** button was clicked or a event notification was sent since last JSON data generation.

> **⚠️ DEPRECATED** — The requirements below have been removed and must NOT be implemented. Retained for historical reference only.

<del>

#### `AGT.2.4.4`

Create status JSON data, retain it on RAM and send it.

</del>

#### `AGT.2.4.5`

Check the device registration status. (Refer "AGT.2.4.40")

### AGT.2.4.10 — Creating status JSON data

#### `AGT.2.4.11`

Obtain the necessary information and generate status JSON data.

#### `AGT.2.4.11.1`

Since second information obtaining in the same time band, update only status that is changed since last time.

#### `AGT.2.4.11.2`

#### `AGT.2.4.11.3`

Save all status information as JSON data.

#### `AGT.2.4.12`

Set the status type in the status JSON data to "Periodic Notification" (sts_type = 0).

#### `AGT.2.4.13`

When a error is occurred while gathering each items for status check, Just keep the last condition. Don't process the error as agent error. Don't update the item that we failed getting.

### AGT.2.4.20 — Retaining status JSON data

#### `AGT.2.4.21`

Generate status JSON data, and retain it on RAM.

> **⚠️ DEPRECATED** — The requirements below have been removed and must NOT be implemented. Retained for historical reference only.

<del>
### AGT.2.4.30 — Sending status JSON data

</del>

#### `AGT.2.4.31`

Send JSON data retained on RAM to AdminLink server(refer to AGT.2.6).

### AGT.2.4.40 — Check the device registration status.

#### `AGT.2.4.41`

Check the device registration status.
But, if any other process to check the device registration status is ongoing, don't execute this process.

By same methond as one of AGT.1.4.

### AGT.2.4.50 — When the device registration status is "Registered".

#### `AGT.2.4.51`

Send the status JSON held in “AGT.2.4.20”.

#### `AGT.2.4.52`

Log the result of the registration status confirmation as **"registered"**.
See the 'Log Message' sheet.

### AGT.2.4.60 — When the device registration status is "Checking".

#### `AGT.2.4.61`

Log results. See the 'Log Message' sheet.

### AGT.2.4.70 — When the device registration status is "Unregistered".

#### `AGT.2.4.71`

Stop periodical process if it is running.
Stop remote control reception.

#### `AGT.2.4.72`

Log the fact that the agent function will stop when it is "unregistered", due to the hourly registration status confirmation. See the 'Log Message' sheet.

## AGT.2.5 — Must be able to monitor the device by polling every minute and send event occurrences to the AdminLink server.

**Rationale:** Client wants to monitor events.

### AGT.2.5.0 — Perform the polling process required for event JSON detection

#### `AGT.2.5.1`

If the AdminLink function is enabled and the device is **"registered"**, a status check is performed every minute at 00 seconds, which is necessary to detect the event JSON.

> **⚠️ DEPRECATED** — The requirements below have been removed and must NOT be implemented. Retained for historical reference only.

<del>
#### `AGT.2.5.2`

Retain the interval (every minute at 00 seconds) on the configuration file.
Don't retain it by hard-coding.

</del>

#### `AGT.2.5.3`

When a event is detected, create a message for each event.

#### `AGT.2.5.4`

When a event is detected, Log it for each detected event.
See the 'Log Message' sheet.

### AGT.2.5.10 — Error in event detection

#### `AGT.2.5.11`

In the case we need getting the device information to detect event occurring,
if fail getting the device information, log it.
See the 'Log Message' sheet.

#### `AGT.2.5.12`

When the status check is failed, just cancel the event detection for the status. Don't process it as agent error.

#### `AGT.2.5.13`

When skipping event detection, don't log it though any other event is detected in the time.
And don't generate event JSON data.

### AGT.2.5.20 — Successful completion of event detection

#### `AGT.2.5.21`

Don't log if the event detection success.

### AGT.2.5.30 — Event detection

#### `AGT.2.5.30.1`

#### `AGT.2.5.31`

When an event is detected, generate a event JSON data.

#### `AGT.2.5.32`

The timing to generate the event JSON data is when the event is detected.

#### `AGT.2.5.33`

#### `AGT.2.5.34`

Generate one event JSON data by one event.

### AGT.2.5.40 — Retaining event JSON data

#### `AGT.2.5.41`

Retain generated event JSON data on RAM.

### AGT.2.5.50 — Creating status JSON data

#### `AGT.2.5.51`

After all event checking is complete, if one or more events are detected and event JSON data is generated, status JSON data will be created.

#### `AGT.2.5.52`

Set the status type of the status JSON data to **"event notification"** (sts_type = 1).

#### `AGT.2.5.53`

If an event that **"needs action"** is detected as **"waiting for action"**, the information of the event JSON is added to the sts[] table of the status JSON.

#### `AGT.2.5.54`

If an event that **"Needs to be dealt with"** is detected as **"Dealt with"**, the information of the event JSON is deleted from the sts[] table of the status JSON.

#### `AGT.2.5.55`

When status JSON data is created, retain it on RAM.

### AGT.2.5.60 — Sending event JSON data and status JSON data

#### `AGT.2.5.61`

Send JSON data retained on RAM to the AdminLink server.

## AGT.2.6 — Must be able to send JSON data to the AdminLink server

**Rationale:** Client wants to send any informations to the AdminLink server as JSON format.

### AGT.2.6.0 — Common specifications for JSON data

#### `AGT.2.6.1`

The data format of the JSON data shall be as follows
- JSON format
- Character code: Unicode(UTF-8)
- With BOM (0xEF, 0xBB, 0xBF)
- The line feed code is LF.
Refer also to the sheet "Upload,Download Functions" on the document "EJ05.(AdminLink)for device side developmentRFP".

### AGT.2.6.10 — Sending JSON data

#### `AGT.2.6.11`

Send JSON data retained on RAM to AdminLink server.
About sending configuration status JSON data, refer AGT.2.8.70 - 2.8.131.

#### `AGT.2.6.12`

When the proxy setting is enabled, communication is done via the proxy.

### AGT.2.6.20 — JSON data sent successfully

#### `AGT.2.6.21`

After JSON data transmission complete, delete the sent JSON data from RAM.

### AGT.2.6.30 — Failed to send JSON data

#### `AGT.2.6.31`

Don't delete the JSON data that is not sent last time. Keep it on RAM.

#### `AGT.2.6.32`

Log transmission errors.
See the 'Log Message' sheet.

#### `AGT.2.6.33`

If there is a unsent JSON data on RAM, retry sending it every 60 seconds.
When the JSON data transmission is successful, delete it from RAM.
If the device fails the retry repeatedly 6 times, don't do the retry again.
A log (AGT.2.6.32) should be recorded for each failed attempt to send JSON data, even in the case of retries.

## AGT.2.7 — A connection client file can be created and sent to the AdminLink server at the time specified by the user.

**Rationale:** Client wants to create the connection client file every specified time.

### AGT.2.7.0 — Execute every user specified time

#### `AGT.2.7.1`

Execute only when **"Remote control permission"** is **"Enabled"** and **"Connected client file upload permission"** is **"Enabled"**.
Do not execute if it is **"Disabled"**.

#### `AGT.2.7.2`

The starting point is the time 00:00:00 + the value of **"number of seconds for calculating the timing of agent information transmission"**, and thereafter it is executed every time specified by the user.
(For example, the case of starting point 00:15:30 and interval every 6 hours, the execute times are 00:15:30, 06:15:30, 12:15:30 and 18:15:30.)

The first execution after user set/changed the configuration or device booted/rebooted, is at the closest to the one of the execution time.
(For example, the case of starting point 00:15:30 and interval every 6 hours, if the user set/changed the configuration at 18:00:00, the next execute time is 18:15:30)

The **"number of seconds to calculate the timing for sending agent information"** is the value of the **"agt_upload_sec"** key included in the response body of the device registration Web API (this value is in seconds).

For the datail about **"agt_upload_sec"**, refer to the sheet "2.2.Device registration API" on the document "EJ03.(AdminLink) 01_ WebAPI Specifications".
For the datail about timing to upload file, refer to the sheet "AGT.2.7.2" on the document "EJ105_AdminLinkRequestSpecifications_APSW".

### AGT.2.7.10 — Create a connection client file

#### `AGT.2.7.11`

Obtain the connection client information of the device and create the data by CSV format.
The file format is UTF-8 with BOM.

AP should include the following information.
- MAC Address
- RSSI
- TX Rate (Mbit/sec.)
- RX RAte (Mbit/sec.)

Switch should include the following information.
- VLAN
- MAC Address
- Port

Define the Switchs 'Connection client information file' as a MAC forwarding Table.
The order of the data should be the same as the Web UI.
The title should be in Japanese.

For the datail about device connection client file, refer to the sheet "Upload,Download Functions" on the document "EJ05.(AdminLink)for device side developmentRFP".

- MAC Address
- RSSI
- TX Rate (Mbit/sec.)
- RX RAte (Mbit/sec.)
- VLAN
- MAC Address
- Port

### AGT.2.7.20 — Failed to create connection client file

#### `AGT.2.7.21`

Log if the creation of the connection client file fails.
See the 'Log Message' sheet.

### AGT.2.7.30 — Successful creation of connection client file

#### `AGT.2.7.31`

### AGT.2.7.40 — Uploading the connected client file

#### `AGT.2.7.41`

Obtain the upload URL using the Web API for obtaining the file upload URL.

#### `AGT.2.7.42`

When the proxy setting is enabled, communication is done via the proxy.

#### `AGT.2.7.43`

Upload the connection client file to the URL you obtained.
About the protocol, refer to the sheet "Upload,Download Functions" on the document "EJ05.(AdminLink)for device side developmentRFP".

#### `AGT.2.7.44`

After the upload is complete, the file upload completion notification Web API is called to notify the AdminLink server of the completion of the upload.
- The file upload type (upload_type) shall be 04 (connection client file).
- The extension shall be "csv".
- In the case of upload by periodical process, set the automatic upload flag (auto_flg) to 1 (automatic).
When uploading by remote control request, set the auto_flg flag to 0 (manual).

### AGT.2.7.50 — Successfully uploaded connection client file

#### `AGT.2.7.51`

Don't Log if the connection client file is successfully uploaded.

### AGT.2.7.60 — Failed to upload connection client file.

#### `AGT.2.7.61`

Log if the upload of the connection client file failed to upload.
See the 'Log Message' sheet.

### AGT.2.7.70 — File upload completion notification Web API failure

#### `AGT.2.7.71`

Log it the execution of the file upload completion notification Web API fails.
See the 'Log Message' sheet.

## AGT.2.8 — Must be able to monitor Web UI configuration changes and send configuration file to the AdminLink server when the configuration is changed.

**Rationale:** Client wants to notify a configuration change to AdminLink server.

### AGT.2.8.0 — Detects that Web UI configuration changes have been applied.

#### `AGT.2.8.0.1`

When the AdminLink function is enabled, and "Configuration file upload permission" is "Enabled" or changed from "Disabled" to "Enabled", monitor the Web UI configuration changes.

#### `AGT.2.8.1`

Detects that a configuration change has been applied in the device's Web UI.

### AGT.2.8.10 — Upload the device configuration file.

#### `AGT.2.8.11`

This function is executed only when "Remote control permission" is "Enabled" and "Configuration file upload permission" is "Enabled".
If **"Disabled"**, it will not be executed.

#### `AGT.2.8.11.1`

When a Web UI configuration change is detected, create device configuration file.
For the datail about device configuration file, refer to the sheet **"Upload,Download Functions"** on the document "EJ05.(AdminLink)for device side developmentRFP".

#### `AGT.2.8.11.2`

After device configuration file creation complete, upload it to the AdminLink server.

#### `AGT.2.8.12`

Obtain the upload URL using the Web API for obtaining the file upload URL.

### AGT.2.8.20 — Successfully retrieved upload URL.

#### `AGT.2.8.21`

Upload the device configuration file to the URL you obtained.
About the protocol, refer to the sheet "Upload,Download Functions" on the document "EJ05.(AdminLink)for device side developmentRFP".

### AGT.2.8.30 — Failed to retrieve upload URL.

#### `AGT.2.8.31`

Log if the Web API to retrieve File Upload URL fails.
See the 'Log Message' sheet.

### AGT.2.8.40 — Successful upload of device configuration file.

#### `AGT.2.8.41`

After the upload is complete, call the file upload completion notification Web API to notify the AdminLink server of the completion of the upload.
- The file upload type (upload_type) shall be 03 (configuration file).
- The extension specifies the extension of the configuration file.
- Set the auto upload flag (auto_flg) to 1 (automatic).
When uploading by remote control request, set the automatic upload flag (auto_flg) to 0 (manual).

#### `AGT.2.8.42`

After the upload is successful, log the message.
See the 'Log Message' sheet.

### AGT.2.8.50 — Failed to upload device configuration file.

#### `AGT.2.8.51`

Log the execution result after the device configuration file upload is executed.
See the 'Log Message' sheet.

### AGT.2.8.60 — File upload completion notification Web API failure

#### `AGT.2.8.61`

Log it the execution of the file upload completion notification Web API fails.
See the 'Log Message' sheet.

### AGT.2.8.70 — Create a configuration status JSON data

#### `AGT.2.8.71`

When the settings included in the configuration status JSON is changed, create configuration status JSON data.
If the configuration status JSON data has already existed, update the changed part on the JSON data.
Regardless of the "Remote control permission" and "Configuration file upload permission" settings, it is always executed when the settings included in the configuration status JSON change.

### AGT.2.8.80 — Upload the configuration status JSON data

#### `AGT.2.8.81`

After the configuration status JSON data has been created, obtain the upload URL using the Web API for URL acquisition for file upload.

### AGT.2.8.90 — Successfully retrieved upload URL.

#### `AGT.2.8.91`

Upload the configuration status JSON data to the obtained URL.
About the protocol, refer to the sheet "Upload,Download Functions" on the document "EJ05.(AdminLink)for device side developmentRFP".

### AGT.2.8.100 — Failed to retrieve upload URL.

#### `AGT.2.8.101`

Log if the Web API to retrieve File Upload URL fails.
No retry, just exit the process.
See the 'Log Message' sheet.

### AGT.2.8.110 — Configuration status JSON data uploaded successfully.

#### `AGT.2.8.111`

After the upload is complete, call the File Upload Completion Notification Web API to notify the AdminLink server of the completion of the upload.
The file upload type (upload_type) shall be 05 (set status JSON file).
The extension is set to "json".
Set the automatic upload flag (auto_flg) to 1 (automatic).

### AGT.2.8.120 — Failed to upload configuration status JSON data.

#### `AGT.2.8.121`

Log the results of the execution after uploading the configuration status JSON file.
See the 'Log Message' sheet.

### AGT.2.8.130 — File upload completion notification Web API failure

#### `AGT.2.8.131`

Log it the execution of the file upload completion notification Web API fails.
See the 'Log Message' sheet.

### AGT.2.8.140 — Create and send status JSON data

#### `AGT.2.8.141`

When the following settings related to remote control are changed, status JSON data will be generated, retained on the RAM, and sent to the AdminLink server.
> **Note:** See "Being able to send JSON data to the AdminLink server".
- Remote control permission
- Configuration file upload permission
- Log file upload permission
- Connection client file upload permission
- Automatic upload interval for connection client files
Regardless of the setting of "Remote control permission", it is always executed when the above settings are changed.

### AGT.2.8.150 —

#### `AGT.2.8.151`

In the case of the following, stop monitoring the configuration changes.
- When the AdminLink function becomes "Disabled".
- When "Configuration file upload permission" becomes "Disabled".
- When the device registration is deleted.

---

